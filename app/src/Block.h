#ifndef _BLOCK
#define _BLOCK

#pragma once
#include "ofMain.h"
#include "Geomancy.h"
#include <string>

using namespace std;

class Block {
    // This is the class that instantiates four tile objects.
    
public:
    Block() {
        // Create a unique name for the array - this is used by making a string out of the counter in the Geomancer class and incrementing it.
        
        std::string tileName = "tile" + string(
        
        // Make a vector to hold tile objects
        
        

        // Create four tile objects and stuff each in the vector
//        for (int i = 0; i < 4; i++) {
////            Tile
//        }
        }
    
};
#endif